services:
  postgres:
    image: postgres:18
    container_name: dulutheats_postgres
    restart: always
    env_file:
      - ../backend/.env
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data: